name: Unlock issue
description: Unlock issue

inputs:
  token:
    description: A token to perform API calls
    required: true
  issue_number:
    description: Issue number to unlock
    required: true

runs:
  using: node12
  main: index.js

branding:
  icon: unlock
  color: green
